 
    --cecho("\n<DimGray>(<cyan>NDB<DimGray>) Don't PANIC, using EQ to Honour the sneaky people!")
if Legacy then
  Legacy.NDB.hiddenUpdate()
end
setTriggerStayOpen("Legacy Hidden Capture", 0)
--disableTrigger("Legacy Hidden Capture")